server:
  port:
    8005

spring:
  application:
    name: restfulapi
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:testdb
  h2:
   console:
      enabled: true
      path: /h2
  jpa:
    defer-datasource-initialization: true